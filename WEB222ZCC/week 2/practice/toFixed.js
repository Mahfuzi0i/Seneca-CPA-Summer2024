/**
 * here, we are going to use toFixed function to encapsulate number
 * this function formats a number to a specific length
 * after its decimal point from the right
 */
var a = 165.111167455;
console.log(a.toFixed(3));
console.log("");
console.log("lets fix the number to 6 precision points-", a.toFixed(6));